# Messages API

Use for constructing or debugging `/v1/messages` requests, message content blocks,
system prompts, token limits, and response stop reasons.

Minimum checks: model, `messages` order, content-block types, `max_tokens`, and error
response. Do not treat local Claude Code CLI behavior as a Messages API contract.

Official entry point: https://docs.anthropic.com/en/api/messages
