# Claude Agent SDK

Use for building an independent agent application with the Claude Agent SDK, including
tools, permissions, hooks, and programmatic sessions.

First distinguish the Agent SDK from the Claude Code CLI. Read only the language and API
sections required by the task; do not copy the SDK documentation in bulk.

Official entry point: https://docs.anthropic.com/en/docs/claude-code/sdk
