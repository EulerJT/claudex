# Streaming

Use for Messages API SSE streams, event order, incremental content blocks, usage, and
terminal handling.

Handle non-terminal deltas, exactly one terminal, disconnection, error events, and the
complete body lifecycle. Never infer successful completion from one delta.

Official entry point: https://docs.anthropic.com/en/api/messages-streaming
